{
  "name": "wintrust-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc",
    "start": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@types/node": "^18.0.0",
    "@vercel/node": "^3.0.0",
    "@vercel/postgres": "^0.10.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vercel": "^32.0.0"
  }
}
